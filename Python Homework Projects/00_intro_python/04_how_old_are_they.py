def main():
    # Step-by-step age assignments based on the riddle
    anton = 21
    beth = anton + 6
    chen = beth + 20
    drew = chen + anton
    ethan = chen

    # Output in the required format (match the capitalization and structure exactly)
    print("Anton is " + str(anton))
    print("Beth is " + str(beth))
    print("Chen is " + str(chen))
    print("Drew is " + str(drew))
    print("Ethan is " + str(ethan))

# Required to call main() when script runs
if __name__ == '__main__':
    main()
